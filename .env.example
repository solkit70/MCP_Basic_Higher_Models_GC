# MCP Web Application - Environment Configuration Example
#
# Copy this file to .env and update with your actual values
# Never commit .env to version control!

# ============================================================================
# MCP Server Configuration
# ============================================================================

# MCP transport mode (stdio or ws)
MCP_MODE=stdio

# Path to MCP server executable
# Format: <python_path> <server_script_path>
# Example (Windows): C:\Python311\python.exe C:\path\to\file_server.py
# Example (Linux): /usr/bin/python3 /path/to/file_server.py
MCP_EXEC_PATH=python c:\AI_study\Projects\MCP\MCP_Basic_Higher_Models_GC\05-build-server\file_server.py

# Default timeout for MCP operations (seconds)
MCP_TIMEOUT_DEFAULT=10

# ============================================================================
# Application Configuration
# ============================================================================

# Application environment (development, staging, production)
APP_ENV=development

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Number of Uvicorn workers (production only)
# Set to number of CPU cores for optimal performance
WORKERS=1

# ============================================================================
# Server Configuration
# ============================================================================

# Server host (0.0.0.0 for all interfaces, 127.0.0.1 for localhost only)
HOST=127.0.0.1

# Server port
PORT=8000

# Enable auto-reload (development only)
RELOAD=True

# ============================================================================
# Security (Production)
# ============================================================================

# API keys (comma-separated)
# API_KEYS=key1,key2,key3

# CORS allowed origins (comma-separated)
# CORS_ORIGINS=http://localhost:3000,https://app.example.com

# Rate limiting (requests per minute)
# RATE_LIMIT_PER_MINUTE=100

# ============================================================================
# Monitoring & Logging (Production)
# ============================================================================

# Enable Prometheus metrics
# ENABLE_METRICS=true

# Metrics port
# METRICS_PORT=9090

# Log file path (empty = stdout only)
# LOG_FILE=logs/app.log

# Log rotation
# LOG_MAX_BYTES=10485760  # 10MB
# LOG_BACKUP_COUNT=3

# ============================================================================
# Database (Future)
# ============================================================================

# DATABASE_URL=postgresql://user:password@localhost:5432/mcp_db

# ============================================================================
# External Services (Future)
# ============================================================================

# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...

# ============================================================================
# Development Tools
# ============================================================================

# Enable debug mode (development only)
DEBUG=True

# Python path for debugging
# PYTHONPATH=.

# ============================================================================
# Docker Specific
# ============================================================================

# Container timezone
# TZ=UTC

# Python buffer mode
PYTHONUNBUFFERED=1

# ============================================================================
# Notes
# ============================================================================

# 1. Never commit .env files to version control
# 2. Use environment-specific files (.env.development, .env.production)
# 3. Store sensitive values in secure vaults (AWS Secrets Manager, Azure Key Vault)
# 4. Validate all environment variables on startup
# 5. Provide sensible defaults for non-sensitive values
